const appConfig = {

    // The Google Analytics ID for the property.
    ga_id: 'UA-153105979-2',

    // Whether or not to show the SHARE button in the post-game screen.
    show_share: false,

    // Determines how the SHARE button in the post-game screen works.
    // true = opens a facebook share dialog sharing the link in "assets/strings/common/games.json ['share_link']"
    // false = opens a new window to the link in "assets/strings/common/games.json ['share_link']"
    share_enabled: false,

    // Whether or not the privacy policy links will be shown in the main menu.
    show_privacypolicy: false,

    // The link to the assets folder. Change this if the assets are hosted separately from the JS files.
    path: 'assets/',
}